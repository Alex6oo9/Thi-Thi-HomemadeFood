// All business info is now managed via Settings → Business Settings and stored in MongoDB.
// This file is kept only so existing imports don't break during any transition.
export const defaultBusinessInfo = {};
export const businessInfo = defaultBusinessInfo;
